#ifndef PARAMS_H
#define PARAMS_H

#include <map>
#include <string>

const std::map<std::string, std::string> DEFAULT_PARAMS = {
  {"MapboxSettings", ""},
  {"IsMetric", "false"},
  {"MapboxToken", ""}
};

#endif
